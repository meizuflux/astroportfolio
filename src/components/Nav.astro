---
const links = [
    {"name": "about", "url": "/about"},
    { "name": "projects", "url": "/projects"},
    { "name": "blog", "url": "/blog"}
]
---
<header>
    <nav>
        <a href="/">meizuflux</a>
        [ {links.map((p, i) => (<a href={p.url}>{p.name}</a>{i < (links.length - 1) ? ", " : ''}))} ]
    </nav>
</header>