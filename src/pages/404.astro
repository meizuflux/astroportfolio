---
import BaseLayout from "../layouts/BaseLayout.astro"
import { makeRequest } from "../lib"

const { articles } = await makeRequest(
    `query MostRecentArticle {
        articles(first: 1, orderBy: published_DESC) {
            slug
        }
    }`
)
const latestPost = articles[0]
---
<BaseLayout title="404" description="Ooh, this doesn't look like the right page.">
    <h1>Hey, it looks like you got lost.</h1>
    <p>Use the links on the navbar to view the pages of this site, or view my <a href=`/blog/${latestPost.slug}`>latest blog post</p>
</BaseLayout>